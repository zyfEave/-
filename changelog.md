## 3.1

- 重构 native 定时主链路为单 timerfd、单 epoll、单 eventfd 和最小堆调度。
- 新增 supervisor 崩溃自恢复、worker 回收、并发限制和 pending-once 合并策略。
- 增强 schedule reload 原子性、clock 探测日志、短时 wakelock 和 stale lock 清理。
- 开机保护期内跳过息屏命令，避免刚开机卡在首屏时被直接熄屏。
- 替换增强版 WebUI，补充 doctor 状态字段和 scheduler 操作入口。
- 补充卸载清理、日志轮转、双 ABI 构建与二进制版本自检。

## 2.5

- 修复部分机型息屏深睡后定时延迟到解锁才继续执行的问题。
- native 定时分支启动后持有唤醒锁，关闭自动定时或重载时释放。
- 执行脚本增加命令超时、非安全锁屏处理和更完整的中文步骤日志。
- WebUI 增加调度器状态显示和一键滚动到底部按钮。

## 2.4

- 调度器改为管理模式，优先使用 native timerfd/epoll 低开销定时器。
- 没有 bin/autofire_timed 时自动降级为 shell 兜底定时，避免定时功能失效。
- 任务执行、定时器、service 和配置保存日志改为中文。
- 新增 native/autofire_timed.c 和 native/build-android.sh，方便编译开源 arm64 定时器。
- 增加调度器启动锁和 native 启动自检，异常时自动切换 shell 兜底。
## 2.3 - 2026-04-29 19:49:10

- 稳定版

## 2.2 - 2026-04-29 19:04:52

- 修复开机后存在旧的间隔状态时，计划任务会立即执行的问题。
- 刷入后默认禁用自动计划任务；需要在 WebUI 中手动启用。
- 仅当自动计划任务已启用时，才从 `service.sh` 启动调度器。
- 增加 180 秒的调度器开机宽限期，避免桌面刚出现就立刻锁屏。

## 2.1 - 2026-04-29 18:39:42

### 更新内容

- 修复部分环境下服务启动失败的问题
- 优化 Web 页面显示效果
- 调整模块配置文件

### 说明

该版本主要是修复和优化更新，建议所有用户升级。

---

## v1.0 - 2026-04-29

### 更新内容

- 初始版本发布
- 添加基础模块结构
- 添加服务启动脚本
- 添加 Web 页面文件

### 说明

第一个公开测试版本。
